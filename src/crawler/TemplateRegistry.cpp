#include "search_engine/crawler/templates/TemplateRegistry.h"

using namespace search_engine::crawler::templates;

// Intentionally empty translation unit to ensure header-only registry
// is compiled/linked when added to the crawler library. Future logic
// for persistence/loading can live here.


