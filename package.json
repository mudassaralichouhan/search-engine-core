{
  "name": "search-engine-core",
  "version": "1.0.0",
  "description": "Search Engine Core",
  "scripts": {
    "format": "prettier --write \"docs/**/*.{json,md}\" \"*.{json,md}\" \".prettierrc.json\"",
    "format:check": "prettier --check \"docs/**/*.{json,md}\" \"*.{json,md}\" \".prettierrc.json\"",
    "validate-schema": "node scripts/validate-schema.js"
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  }
} 